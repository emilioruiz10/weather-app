# 📂 Project Structure

```
final-project-alpha/
│── .github/                    # Stores GitHub-specific configs like workflows and PR templates
│── .vscode/                    # Holds VS Code settings and workspace configs
│── public/                     # Static assets
|── |── images/                 # Static assets images
│── src/                        # Source Code
│   │── components/             # Reusable Global Components
│   │── layout/                 # Layout Component (e.g. Header, Footer, etc...)
│   │── pages/                  # Pages(e.g. Home, Contact, etc...)
│   ├── global.css              # Global css file including tailwind css configuration
│── .gitignore                  # Git ignored files
│── .prettierrc.json            # Prettier Configuration
│── astro.config.mjs            # Astro Configuration
│── biome.json                  # Linter & Format Configuration
│── package-lock.json           # Lock Files
│── package.json                # Dependencies and scripts
│── README.md                   # Project documentation
│── tsconfig.json               # Typescript configuration(Just For using Astro)
```
